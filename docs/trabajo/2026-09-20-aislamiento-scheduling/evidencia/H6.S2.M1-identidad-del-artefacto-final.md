# H6.S2.M1 — La identidad del artefacto final, entregada sin reempaquetar

> **La microtarea sigue `DESCARTADO`, y con razón.** Coordinación decidió no reempaquetar, y ésa es
> una decisión tomada, no un insumo que falta: es exactamente la excepción que la regla 65 §4 deja
> en pie. Lo que **no** es aceptable es que el `DESCARTADO` se lleve puesto el entregable. Este
> archivo entrega la sustancia de la microtarea —**la identidad verificable de la versión final**—
> sin producir un paquete nuevo.

- Fecha: 2026-09-20 · Repo: `mantra-core-health-api`

## 1. Qué se midió

```text
$ git rev-parse 5d5007fb:src/modules/scheduling
c05619f7fdeca7764308e86fb93000976ac12f7f        ← el corte del artefacto v0.1.0-transitional

$ git rev-parse origin/dev:src/modules/scheduling
61304e7dcb709dc80f7c4c2aef864ff6a8c6c21c        ← dev al 2026-09-20

$ git ls-tree -r --name-only 5d5007fb src/modules/scheduling | wc -l → 99
$ git ls-tree -r --name-only origin/dev  src/modules/scheduling | wc -l → 101
```

El `tree` de git es una identidad **reproducible por cualquiera** desde el repo, y no arrastra
fechas ni permisos — que es justamente el defecto por el que el hash del `tar` se había descartado
(ver la corrección en el daily de Itzan §3).

## 2. Las tres respuestas que la microtarea tenía que dar

| Pregunta | Respuesta | Respaldo |
|---|---|---|
| ¿Cambió el artefacto respecto del empaquetado? | **Sí, pero sólo por agregado.** 2 archivos nuevos bajo `ports/` y 2 modificados en `services/`; **506 inserciones, 0 borrados** | `H5.S1.M1-desfase-medido-contra-dev.txt` |
| ¿Cambió el **contrato**? | **No.** Blob del puerto: `4e262747…` en los dos lados, byte a byte | mismo archivo |
| ¿Cambió la **composición**? | **No.** `scheduling.module.ts` idéntico en los dos lados | mismo archivo |

## 3. El número que necesita quien reempaquete

Si mañana coordinación levanta la decisión, el artefacto final **no sale del corte viejo**: sale de
`61304e7dcb709dc80f7c4c2aef864ff6a8c6c21c` (101 archivos). El hash de contenido publicado
—`21fe553b9a97efc9e42d6468b6e7e4d8b34a4a1952a5e79e2c535f360530594a`— **sigue siendo válido para
`v0.1.0-transitional` y no vale para la versión de hoy**: hay 506 líneas que no estaban.

La etiqueta correcta para esa versión futura sería `v0.1.1-transitional`, no una `v0.2.0`: el
cambio es **aditivo y no toca el contrato**, así que ningún consumidor del puerto se rompe.

## 4. Lo que este archivo NO afirma

1. **No se empaquetó nada.** No hay un paquete nuevo, ni un tarball, ni un directorio
   `artefacto-h3/` actualizado. La decisión de coordinación se respeta.
2. **No cambia el estado de entrega.** Sigue siendo `TRANSITIONAL_ISOLATION`: las 5 líneas hacia
   `messaging`/`community` y las referencias a `clinical` están en los dos árboles por igual
   (ver `H3.S1.M3-mapa-de-resolucion-corregido.md`).
3. **No se corrió la suite sobre el árbol de `dev`.** Lo medido acá es identidad de contenido, no
   comportamiento: peldaño `DISCOVERED`, no `TESTED`.
