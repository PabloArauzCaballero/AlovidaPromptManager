# H1.S2.M1, M3, M4 -- ORM, versión instalada y puerto de transacción
(descubrimiento por lectura del código; ningún archivo editado)

## M1 -- orm.config.ts
- Globs (:57-61): `dist/modules/**/entities/*.entity.js` · `dist/src/modules/**/entities/*.entity.js`
  · `entitiesTs: src/modules/**/entities/*.entity.ts`
- `TsMorphMetadataProvider`: sí, `:3` import, `:68` asignado a `metadataProvider`.
- Caché de metadata: `:78-82`, `cacheDir: 'node_modules/.cache/mikro-orm'`.
- El "1159 archivos" de `:67,70-71` es prosa de comentario, NO medición vigente -- no se adopta.
- `HistoryMirrorSubscriber` (`:129` registrado, `:7` import; clase en
  `src/orm/subscribers/history-mirror.subscriber.ts:65-207`):
  - `EventSubscriber` que implementa SOLO `afterFlush` (no hooks de arranque per se).
  - Primera vez: arma un registro `tabla_fuente -> {historyTable, sourceIdColumn, versionColumn}`
    contra `information_schema.columns` de `audit.*_history` (`:79-83`), cacheado.
  - Por cada CREATE/UPDATE con historial registrado, fuera de `EXPLICITLY_WIRED_SOURCES`
    (`medication_requests`, `appointment_bookings`, `:34-37`) y fuera del schema `audit`, cierra la
    revisión vigente y sella una nueva con SQL crudo DENTRO de la misma transacción (`:186-197`).
  - **Fail-closed**: si el INSERT/UPDATE de historial falla, relanza y aborta la transacción de
    negocio (`:199-204`).

## M3 -- versión instalada (verificada contra node_modules, no asumida)
- `@mikro-orm/core` = 7.1.7 · `@mikro-orm/postgresql` = 7.1.7 · **`@mikro-orm/nestjs` = 7.0.2**
  (el adaptador Nest es una MENOR distinta -- precisión que el prompt no traía).

## M4 -- transaction.port.ts
- `TransactionManager.execute<T>(operation, options?)` existe (`:11-26`).
- `TransactionOptions { isolationLevel?, operation? }` existe (`:29-39`).
- `TransactionContext` NO vive en este archivo: se importa de `./persistence-context.ts:19-22`
  (`{ readonly __transaction: string }`).
- Token: `TRANSACTION_MANAGER = Symbol('TRANSACTION_MANAGER')` (`:42`).
- Implementación real en `postgres-transaction.manager.ts:64-75+`: resuelve conexión vía
  `DataSourceRouter`, exige capacidad `transactions`, envuelve en `PostgresTransactionContext`
  (`__transaction='postgres'`).
- **Propagación entre `fork()`/anidadas: `NOT_RUN`.** Existe un spec
  (`persistence-session.contract.spec.ts:135-139`) que ejercita la forma del contrato, pero con
  `EntityManager` mockeado -- no se corrió, y aunque se corriera no probaría propagación real.
  Declarado `NOT_RUN`, no "funciona" (regla 30).
